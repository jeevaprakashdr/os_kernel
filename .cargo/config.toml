### Workaround to over come Linker error
### Configuration example to build a binary targeting Windows, Linux and MacOS
### there is a better approach https://doc.rust-lang.org/1.16.0/book/no-stdlib.html
# [target.'cfg(target_os = "linux")']
# rustflags = ["-C", "link-arg=-nostartfiles"]

# [target.'cfg(target_os = "windows")']
# rustflags = ["-C", "link-args=/ENTRY:_start /SUBSYSTEM:console"]

# [target.'cfg(target_os = "macos")']
# rustflags = ["-C", "link-args=-e __start -static -nostartfiles"]

[build]
target = "x86_64_os_kernel.json"

[target. 'cfg(target_os = "none")']
runner = "bootimage runner"

[unstable]
json-target-spec = true
build-std-features = ["compiler-builtins-mem"]
build-std = ["core"]